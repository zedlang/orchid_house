<header>
  <h1>Products</h1>
</header>
<body>
  <ul class="orchids">
      <% @products.each do |product| %>
        <h3><%= product.name %></h3>
        <li id="<%= product.id %>">
          <%= link_to "Details", product_path(product) %>
        </li> 
      <% end %>
  </ul>
</body>